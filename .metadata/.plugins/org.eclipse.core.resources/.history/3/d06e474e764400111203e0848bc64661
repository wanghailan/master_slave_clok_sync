/*
 * pwm_slave_sync.c
 *
 * 功能：
 *    Slave 侧 PPS 同步 PWM。
 *    通过 eCAP1 捕获经 PTP 协议同步后的 PPS 上升沿，
 *    在确认 PTP 已锁定（Cpu1Ipc_cm2cpu.PtpSynced==1）后，
 *    校正 EPWM1 相位，使其与 Master 的 PWM 间接对齐。
 *
 * 修改记录：
 *    1. 补上头文件与 .h 宏定义
 *    2. TBCLKSYNC 配置后恢复使能
 *    3. PHASE_OFFSET 统一为 0L
 *    4. 增加 PTP 未锁定保护（PtpSynced 判断）
 *    5. 增加多 EPWM 级联注释与示例
 */

#include "pwm_slave_sync.h"
#include "bsp.h"
